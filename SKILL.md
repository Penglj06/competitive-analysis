---
name: competitive-analysis
slug: competitive-analysis
description: 结构化竞品分析方法论，把竞品分析从「截图堆资料」升级为「支撑产品决策」。仅在用户明确表达竞品分析 / 对标分析 / 差异化分析意图，或明确要求对某个产品、平台、工具做带对比对象的调研时使用；产出经证据分级、借鉴/照抄二分、决策三分类的分析结论。
description_zh: "结构化竞品分析：拆流程、反推策略、借鉴/照抄二分，落到产品决策"
description_en: "Structured competitive analysis: flow decomposition, strategy inference, adopt-vs-copy, land on product decisions"
version: 1.5.2
displayName: "竞品分析"
summary: "把竞品分析从截图堆资料升级为产品决策输入：先定目标与基线，分三层选竞品，拆核心流程，多维对比，反推取舍，借鉴/照抄二分，最后落到立即做/先验证/不做三类决策。全程证据分级，不编指标。通用方法论，已在 AI4S / 学术科研工具竞品上实战验证。"
tags: [竞品分析, 对标分析, 产品决策, 方法论]
license: MIT
homepage: ""
display_name: "竞品分析"
display_name_en: "Competitive Analysis"
agent_created: true
---

# 竞品分析

## 用途

把「竞品分析」从「截图合集 + 功能对比表 + 一句套话结论」升级为**可执行的产品决策输入**。
通过拆流程、反推取舍、借鉴/照抄二分，最终回答的不是「竞品怎么做」，而是
**「看完竞品之后，我们该怎么做」**。

**语言**：输出语言**跟随用户**（用户用中文则中文，用其他语言则用其语言；无法判断时默认中文，
并可主动询问或提供中英双语）。本文件以中文书写，仅为文档语言，不代表强制输出中文。

**范围**：本 skill 只提供分析方法与产出规范，**不安装任何软件、不修改系统环境、
不自行发起网络访问**；需要外部能力时先取得用户同意（见「使用前提」）。

## 何时使用

**仅当用户明确表达竞品分析意图时启动**：明确提出「竞品分析 / 竞品调研 / 对标分析 / 差异化分析」，
或明确要求对某个产品 / 平台 / 工具做**带对比对象**的调研。

**判定原则**：以**用户显式意图**为准，**不做关键词字面匹配、不主动接管**。
用户只是随口聊某个产品、问功能清单或使用教程，都不触发。

**启动前先确认**：即使意图明确，也先用一句话确认「是否按结构化竞品分析流程推进」，
以及是否允许进行外部资料检索 / 页面访问；用户确认后再进入工作流，
**不自行扩大读取、检索或执行范围**。

**不适用**：只查某产品的功能清单 / 使用教程（属信息查询）；只写 PRD / 需求文档（无对比对象）；
纯闲聊式「XX 怎么样」；用户访谈整理、单纯文献综述（无竞品或方案对比）。

---

## 使用前提（依赖与环境）

**本 skill 自身不安装任何软件、不修改系统环境、不写入用户目录之外的路径。**
下面两项均为**可选**外部能力；缺失时对应环节自动降级，核心分析不受影响。

1. **深度走查（可选）依赖环境已具备的浏览器能力**（如宿主提供的页面访问工具）：
   - **本 skill 不负责安装、不提供安装命令**。该能力应由使用者在其受控工作区中**自行预置**。
   - 若环境**未提供**该能力，第 2.5 步直接降级为公开资料（WebSearch），证据等级标「公开」，
     **不要为了走查去自动安装任何依赖**。
   - 若使用者**主动要求**启用：须先取得其明确同意，使用**固定且经审阅的版本**做**项目本地安装**
     （**禁止任何全局安装**），并在隔离的浏览器配置、最小权限下运行，
     不输入个人账号凭据。相关安全注意事项见 `references/analysis-playbook.md` 第十节。

2. **Word 报告（可选）依赖已被单独审阅的 `html-to-docx` 能力**：第 8 步把含图表的 HTML 转 `.docx` 需要它。
   使用前确认该能力已启用、来源已单独审阅；若不可用，降级交付「Markdown + 内嵌图表的 HTML」并说明原因。
   **不要仅为「该 skill 存在」就执行它携带的安装/初始化脚本。**

> 两项都不具备也能跑完「定位 → 分层 → 拆解 → 对比 → 反推 → 二分 → 决策」的核心分析，
> 只是少了「走查证据」和「Word 格式」两样交付物。

---

## 硬性约束（不可违反）

1. **证据分级，全程标注**。每条关于竞品的结论必须标证据等级：
   - **一手**：真实注册使用过、真实用户访谈、拿到真实数据
   - **走查**：用环境提供的页面访问能力实时访问竞品页面看到的内容（页面实际展示的功能/文案/定价）
   - **公开**：官网文案、帮助文档、媒体报道、财报、公开文档、搜索摘要
   - **推断**：AI 推测 → **必须**附「用什么公开信号可佐证或证伪」，不得当作事实陈述
   未标注来源的结论不写入报告。
   > 注意：「走查」≠「一手」。走查只能看到公开可访问的页面，看不到登录后的真实使用体验、
   > 真实数据和真实用户反馈。凡涉及「用户真实体验/效果/内部数据」的结论，走查证据
   > 不能升级为「一手」，只能如实标注「走查所见」。

   **官方自述数字 ≠ 独立核验数字**：官网/宣传页上的规模数字（用户数、文献量、期刊数等）
   常含营销口径放大，与第三方报道/财报可能不一致。凡引用这类数字，一律标「官方自述，
   未独立核验」；发现同一指标不同口径（如官网写 X 万用户、媒体写 Y 万）时，
   在报告里显式列出差异并说明可能原因（时间点不同 / 营销口径 / 统计口径），
   不替用户「选一个当真」。（实测：某平台官网自述用户数是同期媒体报道的数倍。）

2. **绝不虚构指标**。竞品的 DAU、留存、转化率、营收等内部数据**拿不到就是拿不到**，
   禁止编数字。第 5 步「反推策略」只反推**可观察的取舍**（做重了什么、简化了什么、
   牺牲了什么），**不反推指标**。确实需要量化判断时，只能用公开可得的信号
   （如应用商店下载量档位、公开融资新闻、公开财报），并标注来源与时间。

3. **先出我方基线，再谈对比**。没有「我们」的现状快照，「借鉴点为什么适合我们」
   无从判断。分析开始前必须明确我方：用户规模、业务阶段、核心指标、资源约束、品牌心智。
   我方信息缺失时，用 宿主的用户提问能力 补齐，不猜。

4. **定位不清晰或资料不足，先停下来补，禁止带假设往下跑**。用户对自家产品的
   描述若只有一句话、定位模糊，或没提供产品资料，**必须先要求用户澄清定位或提供
   资料，再开始分析**。宁可多问一轮，也不靠假设推进——假设一旦错，后面全偏。
   （实测教训：仅凭「AI4S 学科智能体平台」一句话就往下走，差异化结论一度跑偏，
   直到用户补充产品定义才修正。）

5. **竞品名单必须用户确认后再深挖**。第 2 步初筛出的竞品名单，**必须先回显给
   用户确认**（哪些保留、哪些删、要不要补），确认后才进入第 3 步深度拆解。
   不要替用户做「该分析谁」的最终决定。

6. **深度走查优先于公开资料（有预算上限，且以环境已具备浏览器能力为前提）**。直接竞品若可访问、
   且环境**已提供**页面访问能力，优先实际走查，把证据从「公开」升级为「走查」。公开资料
   （WebSearch）只作补充和交叉验证，**不作为深度分析的唯一来源**。
   走查结果与公开资料的冲突处，以走查为准并注明。
   **走查预算**：默认深走查 1-2 个直接竞品（逐页走核心流程），其余竞品轻走查
   （只开首页+定价页）或公开资料。不为「显得彻底」把所有竞品都深走查——
   走查有环境与时间成本。**环境未提供页面访问能力时，不安装依赖，直接走公开资料路径。**

7. **分步推进，不一次性要结果**。各步按顺序走，每步确认后再进下一步。
   **禁止**把整套分析塞进一个长 Prompt 一次性输出——一步跑偏，后面全带偏。
   每步产出先回显给用户，确认无误再继续。

8. **借鉴 / 照抄必须二分**。每个「竞品做了 X」的判断，最终必须落到
   「我们该借鉴（含改造方案）」或「我们不能照抄（含风险 + 替代方案）」，
   不允许停留在「A 更完整、B 更简洁，我们可以结合参考」这种套话。

9. **时间戳 + 复看周期**。分析是竞品在某一时点的快照，报告必须标注分析日期，
   并对直接竞品给出建议复看周期（默认季度）。

10. **不追求「列满」**。流程节点、对比维度各 5-6 个关键点即可，不为凑满 10 个而堆
   无关项。**结论质量 > 条目数量**。

11. **按任务级别交付报告**。标准/正式分析交付 `.docx`（环境支持时）和 Markdown；快速判断可仅交 Markdown。正式报告必须包含
    **图表**（至少一张格局/定位图或对比图）。**图表一律用 PNG 图片**，不要用 SVG——
    html-to-docx 只支持 PNG/JPEG（本地路径或 base64），SVG 无法进 Word。图的两个来源：
    ① 走查截图（走查时保存的页面截图，PNG，最有证据力）；② 示意图用 Pillow/matplotlib 画 PNG。
    标准/正式分析不要只交 Markdown（快速判断除外）。

12. **当前日期必须由宿主能力获取**。优先使用 clock/current_time；Unix 用 `date +%F`，Windows PowerShell 用 `Get-Date -Format yyyy-MM-dd`，禁止凭记忆推断。

---

## 七步工作流（总览）

> 「七步」指下面 ①~⑦ 七个**核心决策步骤**，对应正文「第 1 步 ~ 第 7 步」。
> 正文里还有 4 个围绕这七步的补充环节，不占「七步」之列：
> **第 0 步**（前置：取日期 + 建我方基线）、**第 2.5 步**（深度走查）、
> **第 8 步**（输出 Word 报告）、**第 9 步**（交付产物清单）。
> 执行时按正文「第 0 步 → 第 9 步」的完整顺序走，勿因「七步」之名漏掉补充环节。

```
① 定目标 ── 解决什么问题 · 支撑什么决策 · 什么信息不该影响决策
② 选对象 ── 分三层（直接 / 间接 / 跨行业），每层定数量与深度
③ 拆流程 ── 不看表面形态，看用户从进入到完成目标的运转路径
④ 多维比 ── 不比「有没有」，比入口·场景·成本·闭环·代价·业务目标
⑤ 反推取舍 ── 做重了什么、简化了什么、牺牲了什么（不反推指标）
⑥ 借鉴/照抄 ── 可借鉴（+怎么改造） vs 不能照抄（+风险与替代）
⑦ 决策输出 ── 立即做 / 先验证（含成功标准）/ 不做
```

三条贯穿原则：**流程 > 页面**、**策略 > 功能**、**迁移 > 复述**。

详细的每一步 Prompt 模板、分层规则、证据分级细则见
`references/analysis-playbook.md`，报告模板见 `assets/report-template.md`。

---

## 工作流

### 第 0 步：取当前日期 + 建我方基线

```bash
date +%F    # 所有日期判断以命令输出为准
```

先明确我方现状（竞品分析的锚点），关键字段缺失时必须暂停询问；补充字段缺失时可显式写入「待确认假设」后继续：

| 我方基线项 | 说明 |
|---|---|
| 产品 / 业务是什么 | 一句话 |
| 当前阶段 | 冷启动 / 增长 / 成熟 / 转型 |
| 用户规模档位 | 量级即可，不必精确 |
| 核心指标 / 北极星 | 或当前最关心的指标 |
| 资源约束 | 团队规模、预算、时间窗 |
| 品牌心智 / 数据能力 | 相对竞品的强弱项 |

**定位门槛（硬性）**：若用户对产品的描述只有一句话、定位模糊，或未提供任何
产品资料（PRD/简介/功能清单），**必须先停下来要求用户澄清定位或补资料**，
用 宿主的用户提问能力 问清「是什么、给谁用、解决什么问题、当前阶段、差异化目标」。
**禁止带假设往下跑**。定位清晰是后续所有对比的锚点。

### 第 1 步：明确分析目标

先不找竞品，先问清楚这次分析要解决什么。让 AI 按以下结构输出（可参考
`references/analysis-playbook.md` 的完整 Prompt）：

1. 这次分析要解决什么产品问题？
2. 最终要帮助团队做什么决策？
3. 哪些信息是必收的？
4. 哪些信息只是参考，不该影响决策？
5. 最终应输出什么结论？
6. 分析做完后，能帮团队做什么决定？

**没有目标的竞品分析，大概率会退化成资料整理。**

### 第 2 步：竞品分层 + 定资源分配

竞品分三类，每类看的内容不同：

| 层级 | 定义 | 看什么 | 默认数量 |
|---|---|---|---|
| **直接竞品** | 用户类似 + 场景类似 + 问题类似 | 深看，全流程 | 2-3 个 |
| **间接竞品** | 用户不同，但解决路径相似 | 借鉴流程、交互、商业模式 | 3-5 个，看单点 |
| **跨行业参考** | 行业不同，但某项能力强 | 只取一个能力，拓思路 | 2-3 个 |

同时明确「不建议分析的竞品类型」（如已停止运营、与目标问题无关、纯营销号包装）。

**名单确认卡点（硬性）**：初筛出的竞品名单（含分层）**必须先回显给用户确认**——
列出「直接 / 间接 / 跨行业」三层名单及选择理由，问用户「哪些保留、哪些删、要不要补」。
用户确认后才进入第 3 步。不要替用户做最终决定。

### 第 2.5 步：深度走查（优先于公开资料，前提是环境已具备页面访问能力）

对用户确认过的**直接竞品**，若可访问、且环境已提供页面访问能力，优先做实际走查。

**走查前先取得用户同意（硬性）**：访问竞品页面属于对外部网络的操作。**先向用户确认是否允许**，
只走查**用户提供的 URL**；执行前做基本校验——域名/主体与目标竞品一致，**排除同名站、仿冒站、
蹭名站、第三方平台同名条目**，明显对不上时先用提问能力核对，别直接访问。

**本 skill 不安装、不配置任何浏览器工具**。若环境**已提供**页面访问能力，按其既有用法
逐页走核心流程（打开页面 → 等待加载 → 抓取页面文本与可交互元素 → 关键页截图 → 记录实际功能/交互 →
结束后关闭会话）；**若环境未提供，直接降级公开资料，不自行安装依赖**。

**SPA 页面降级 + 空数据判断（硬性）**：很多现代站点是单页应用（SPA），等待网络空闲可能一直挂起或拿到空内容。走查时：

1. 等待超时 → 降级为只等页面加载完成；
2. 拿不到内容时先探测页面正文长度与脚本/图片数量；
3. 正文极短、或出现 `{"status":0,"data":{}}` 之类接口空返回、且脚本/图片数为 0，
   说明是**登录墙 + 动态渲染**，未登录拿不到真实数据；
4. 命中登录墙 → 如实标「无法走查」，降级为公开资料（WebSearch），在报告证据边界说明，
   不要硬凑。要不要换走查目标（如正牌官网）先问用户，别自行替换。

- 走查产出：关键页面截图（PNG）+ 实际功能/流程记录，证据等级标「**走查**」（不是「一手」）。
- **走查预算**：默认深走查 1-2 个直接竞品（逐页走核心流程），其余竞品轻走查
  （只开首页+定价页）或公开资料。不为「显得彻底」全量深走查。
- 公开资料（WebSearch）只作补充与交叉验证，**不作为深度分析的唯一来源**。
- 竞品无法访问（登录墙/需付费/地区限制）时，如实标注「无法走查」，降级为公开资料，
  并在报告的证据边界里说明。
- 走查结果与公开资料冲突时，以走查为准并注明。
- 涉及「用户真实体验/效果/内部数据」的结论，走查看不到，如实标「未验证/无数据」，
  不得把走查所见拔高成「一手」。

### 第 3 步：拆核心流程（先流程后截图）

截图只能告诉你页面长什么样，流程才能告诉你产品怎么运转。拆用户从进入
到完成目标的完整路径：入口 → 引导 → 核心任务开始 → 关键页面 → 操作路径 →
系统反馈 → 结果输出 → 复用/留存 → 付费/转化 → 关键设计意图。

**流程拆完后，再谈截图才有意义**——因为你知道每张图在流程里的位置。

### 第 4 步：多维功能对比（不比「有没有」）

「有没有」是最浅一层。真正要对比的是：功能是否存在 → 入口在哪 → 面向什么场景 →
解决什么问题 → 操作成本高不高 → 是否形成闭环 → 对体验的影响 → 对业务目标的影响 →
是否值得借鉴 → 借鉴时注意什么。

**大坑提醒**：竞品有，不代表我们必须有；竞品没有，不代表我们不能做。
关键是看它背后的场景和目标。

### 第 5 步：反推取舍（不反推指标）

分析竞品背后的产品策略，但**只看可观察的取舍，不编指标**：

1. 目标用户是谁？
2. 核心使用场景是什么？
3. 它把哪些事情做得很重？
4. 它把哪些事情刻意简化了？
5. 它可能牺牲了什么体验？
6. 它的商业化 / 转化设计在哪里？
7. 它的取舍给我们什么启发？
8. 哪些地方不能直接照抄？

> 成熟产品的每个入口、按钮、默认选项背后都有取舍。产品经理要看取舍，不是看表面。
> 若用户坚持要量化判断，只用公开信号（下载量档位、融资新闻、财报）并标注来源时间。

### 第 6 步：借鉴 / 照抄二分（价值峰值）

这是把「信息整理」变「决策输入」的关键一步。竞品能这么做，可能因为用户规模、
业务阶段、数据能力、品牌心智、商业模式、资源投入都不同。

| 类别 | 输出内容 |
|---|---|
| **可借鉴** | 借鉴点是什么 · 解决什么问题 · 为什么适合我们 · 落地如何改造 |
| **不能照抄** | 竞品怎么做的 · 为什么不能直接抄 · 照抄的风险 · 我们的替代方案 |

### 第 7 步：决策输出（三分类）

| 类别 | 输出内容 |
|---|---|
| **建议立即优化** | 优化项 · 原因 · 预期收益 · 实现复杂度 |
| **建议先验证** | 验证什么 · 验证方式 · **成功标准** · 复盘点 |
| **不建议照抄** | 竞品做法 · 不做的原因 · 替代方案 |

最后给出：① 核心结论 ② 对我们产品最重要的 3 个启发 ③ 下一步行动建议 ④ 可写入 PRD 的内容。

**「先验证」必须带成功标准 + 复盘点**，否则落地后没有回路，分析就白做了。

### 第 8 步：输出报告（正式分析交 Word，含图表用 PNG）

按任务级别交付：标准/正式分析以 `.docx` 交付（环境不支持 html-to-docx 时降级为 Markdown），
快速判断可仅交 Markdown。**正式报告必须包含图表**（至少一张格局/定位图或对比图）。
**图表一律用 PNG**（html-to-docx 只支持 PNG/JPEG，SVG 进不了 Word）。两个来源：

1. **走查截图**（首选）——走查时保存的关键页面截图（PNG），直接 `<img src="本地路径">` 引用，
   既是「走查证据配图」，又满足「含图表」要求。
2. **示意图**（格局分层图/差异化定位图）——用 Pillow 或 matplotlib 画成 PNG，
   再 `<img>` 引用。**不要用 widget 的 SVG**（进不了 Word）。

转换步骤（文字流程，具体命令依宿主提供的转换能力而定）：

1. 生成 PNG 图（走查截图已现成；示意图用 Python 画）；
2. 把报告结构化为 HTML，图用 `<img src="/abs/path.png">` 本地路径引用；
3. 用**已启用、且来源已单独审阅**的 `html-to-docx` 能力把 HTML 转 `.docx`
   （**不要仅为「该 skill 存在」就执行它携带的安装/初始化脚本**）；
4. 用宿主的文件呈现能力交付 `.docx`，保留 Markdown 源便于编辑。

> 图表要求：至少一张。优先「竞品格局分层图」「我方差异化定位图」或「走查关键页截图」，
> 让读者一眼看清「竞品在哪、我们插在哪」。

### 第 9 步：交付产物清单

一次完整竞品分析的标准产物集（写进工作区，建议归入 `<主题>/` 子目录）：

| 产物 | 格式 | 说明 |
|---|---|---|
| 主报告 | `.docx` + `.md` 源 | 核心交付物，含图表 |
| 走查记录（如走查了） | `.md` | 直接竞品的走查记录（**走查证据，非一手**，见证据分级） |
| 典型任务/清单（如适用） | `.md` | 拆出的可产品化清单 |

文件名统一带主题前缀（如 `<主题>竞品分析报告.docx`），不要用「报告.docx」「分析.docx」
这类无主题名。走查截图 PNG 归入 `<主题>/screenshots/` 子目录，不散落在根目录。

---

## 失败与降级路径

### 高频故障速查（先查这里）

| 现象 | 最可能原因 | 怎么办 |
|---|---|---|
| 环境未提供页面访问能力 | 未预置浏览器工具 | **不自行安装**；直接降级公开资料（WebSearch），证据标「公开」 |
| 页面打开了但没内容 / 只看到 `{"status":0}` | SPA 动态渲染 + 登录墙 | 等待降级为「页面加载完成」；仍为空则标「无法走查」，转公开资料 |
| Word 里的图不见了 | 用了 SVG，或图片用了相对路径 | 图表一律 PNG + 绝对路径；SVG 进不了 Word |
| 无法转 Word | `html-to-docx` 能力未启用或未审阅 | 降级交付 Markdown + 内嵌图表 HTML，并说明 |
| 走查拿到的数字与公开报道打架 | 官方自述口径放大 | 如实并列两个口径，标「官方自述未核验」，不下对错结论 |

详细规则见下方各条。

- **定位不清晰 / 没给资料**：停下来，宿主的用户提问能力 问清定位或要求补资料，
  不靠假设推进。用户拒绝澄清时，明确告知「结论将基于假设，可能跑偏」，让用户知情。
- **缺我方信息**：宿主的用户提问能力 补齐基线，关键字段不回答则暂停；补充字段不回答则标注「待确认假设，可随时调整」。
- **竞品无法走查**（登录墙/付费/地区限制）：如实标注「无法走查」，降级为公开资料，
  报告证据边界里说明。
- **走查看不到的东西**（真实使用体验/效果/内部数据）：不硬凑，标「未验证/无数据」，
  不得把走查所见拔高成「一手」。
- **拿不到竞品一手信息**：公开资料 + 推断档处理，推断结论必须标「推断」+ 附佐证信号。
  禁止为「显得深入」而把推断写成事实。
- **竞品公开信息极少**（如学术产品查不到 DAU）：不强求量化，改为反推
  「它服务的研究/业务流程位点是哪一段」，这个从产品形态可观察到，比编指标可靠。
- **用户想一次要结果**：先解释「分步走，一步跑偏全带偏」，坚持逐步骤交付；
  若用户仍坚持，则按工作流骨架一次输出，但每段明确标证据等级。
- **无法转 Word**（`html-to-docx` 能力未启用或未审阅）：降级交付 Markdown + 内嵌图表的 HTML，并说明原因。
- **示意图无法转 PNG**（无 matplotlib/Pillow）：用走查截图当图；连截图也没有时，
  用 Markdown 表格替代示意图，但报告头注明「本版无图」。

---

## 输出质量要求

- **证据分级**：每条结论标「一手 / 走查 / 公开 / 推断」四档，推断附佐证或证伪信号；
  走查结论优先于公开资料，但不得冒充「一手」
- **不编指标**：拿不到 DAU、留存、转化就明说「内部数据不可得」，只给公开信号
- **结论落到「我们」**：不以「竞品怎么做」结尾，以「我们该怎么做 / 不做什么」结尾
- **借鉴/照抄二分**：每个关键判断都归入可借鉴或不能照抄，附改造方案或风险替代
- **带时间戳**：报告标分析日期，直接竞品给复看周期
- **结构化 + 含图表（PNG）**：**输出语言跟随用户语言**（用户用中文则中文，用其他语言则用其语言；无法判断时默认中文，并可询问或提供双语选项）；表格 + 分层列表 + 至少一张 PNG 图，少空话、少套话
- **交付 Word**：`.docx` 为主交付物（含 PNG 图），保留 Markdown 源
- **产物清单**：按第 9 步约定产出，文件名带主题前缀，截图归入子目录

## 资源

- `references/analysis-playbook.md` —— 七步完整 Prompt 模板、分层与资源分配细则、
  证据分级标准、反推取舍的提问清单、借鉴/照抄二分模板、深度走查操作指引
- `assets/report-template.md` —— 竞品分析报告结构模板（含证据等级列、我方基线、
  决策三分类、时间戳与复看周期、图表占位）
- **外部能力（均为可选，由环境预置，本 skill 不安装）**：
  - 页面访问 / 走查能力——环境已提供时用于直接竞品走查；未提供则走公开资料
  - `html-to-docx` 能力——第 8 步输出 Word 时使用，须来源已单独审阅；不可用则降级 Markdown
