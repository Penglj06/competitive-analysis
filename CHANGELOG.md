# 更新日志

本 skill 从学习方法论文章起步，经多轮真实竞品走查迭代。以下记录关键演进。

## 1.3.1（2026-09-07）

面向社区分享的可用性与一致性修复：

- **新增「使用前提（依赖与环境）」**：明确深度走查依赖 `agent-browser`（需 `npm install` + 下载 Chromium）、Word 报告依赖 `html-to-docx` skill；说明不装也能跑核心分析，只是缺两项交付物
- **修正证据等级表述**：第 8 步「一手证据配图」改为「走查证据配图」，与全文「走查≠一手」原则对齐
- **新增「高频故障速查」表**：`command not found` / SPA 空数据 / Word 丢图 / token 环境缺失 / 数字口径冲突，五个高频坑的现象-原因-怎么办
- **不触发清单标注优先级最高**：与触发词冲突时以不触发为准，避免边缘场景误触发
- 新增本 CHANGELOG 与 `examples/` 示例报告

## 1.3.0（2026-09-06）

一次真实竞品走查暴露的三个漏洞修复（走查对象已去名化）：

- **走查前核对竞品主体**：用户给的链接可能是同名站 / 仿冒站 / 蹭名站 / 第三方平台同名 Agent，域名对不上先用 `AskUserQuestion` 核对，避免空转
- **SPA 降级 + 空数据判断**：`networkidle` 挂起时降级 `wait --load load`；`bodyText` 极小、接口返回 `{"status":0}` 且无脚本无图片 = 登录墙，如实标「无法走查」并降级公开资料
- **官方自述数字 ≠ 独立核验**：官网规模数字标「官方自述未核验」，同一指标不同口径如实并列，不替用户选一个当真

## 1.2.0（2026-09-06）

证据与交付链路完善：

- 证据分级扩展为四档「一手 / 走查 / 公开 / 推断」，明确「走查 ≠ 一手」
- 图表改为 PNG（html-to-docx 只支持 PNG/JPEG，SVG 进不了 Word）
- 新增走查预算（默认深走查 1-2 个，其余轻走查）
- 理清「走查截图 = 证据配图」与「示意图 = 格局图」两个来源
- 新增第 9 步交付产物清单约定（文件名带主题前缀、截图归子目录）

## 1.1.0（2026-09-06）

- 加「定位门槛」硬约束：定位不清或没资料必须先停下问清，禁止带假设往下跑
- 加「竞品名单用户确认卡点」与「第 2.5 步深度走查」
- 加「第 8 步输出 Word 报告（含图表）」

## 1.0.0（2026-09-06）

初版落成：七步工作流 + 证据分级 + 借鉴/照抄二分 + 决策三分类。
